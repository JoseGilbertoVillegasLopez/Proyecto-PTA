<turbo-frame id="content">
	<section class="proceso-estrategico-form proceso-estrategico-form--edit">

		<header class="proceso-estrategico-form__header">

			<a href="{{ path('app_proceso_estrategico_index') }}"
			   data-turbo-frame="content"
			   data-turbo-action="replace"
			   class="proceso-estrategico-form__icon-btn proceso-estrategico-form__icon-btn--back"
			   title="Volver">
				<i class="bi bi-arrow-left"></i>
			</a>

			<div class="proceso-estrategico-form__header-center">
				<h1 class="proceso-estrategico-form__title">
					Editar proceso estratégico
					{{ proceso.nombre }}
				</h1>

				<p class="proceso-estrategico-form__subtitle">
					Edita solo los campos necesarios
				</p>
			</div>

		</header>

		{{ form_start(form, {
			action: path('app_proceso_estrategico_edit', { id: proceso.id }),
			attr: {
				'data-turbo-frame': 'content',
				'class': 'proceso-estrategico-form__form'
			}
		}) }}

		<div class="proceso-estrategico-form__group">
			{{ form_label(form.nombre) }}
			{{ form_widget(form.nombre) }}
			{{ form_errors(form.nombre) }}
		</div>

		<div class="proceso-estrategico-form__group">
			{{ form_label(form.activo) }}
			{{ form_widget(form.activo) }}
			{{ form_errors(form.activo) }}
		</div>

		<div class="proceso-estrategico-form__submit-proxy">
			<button type="submit"
			        class="proceso-estrategico-form__icon-btn proceso-estrategico-form__icon-btn--save"
			        title="Guardar cambios">
				<i class="bi bi-check-lg"></i>
			</button>
		</div>

		{{ form_rest(form) }}
		{{ form_end(form) }}

	</section>
</turbo-frame>