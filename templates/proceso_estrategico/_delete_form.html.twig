<form method="post" action="{{ path('app_proceso_estrategico_delete', {'id': proceso_estrategico.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proceso_estrategico.id) }}">
    <button class="btn">Delete</button>
</form>
