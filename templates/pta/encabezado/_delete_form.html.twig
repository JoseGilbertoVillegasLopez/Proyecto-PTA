<form method="post" action="{{ path('app_encabezado_delete', {'id': encabezado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ encabezado.id) }}">
    <button class="btn">Delete</button>
</form>
