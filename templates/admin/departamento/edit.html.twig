<turbo-frame id="content">
	<section class="departamento-edit-page-card">

		<header class="departamento-edit-page-header">

			<!-- VOLVER -->
			<a href="{{ path('app_departamento_index') }}"
			   data-turbo-frame="content"
			   data-turbo-action="replace"
			   class="departamento-edit-page-back-btn"
			   title="Volver">
				<i class="bi bi-arrow-left"></i>
			</a>

			<!-- TÍTULO + SUBTÍTULO -->
			<div class="departamento-edit-page-header-center">
				<h1 class="departamento-edit-page-title">
					Editar departamento
				</h1>

				<p class="departamento-edit-page-subtitle">
					Edita solo los campos necesarios
				</p>
			</div>

			<!-- ESPACIADOR -->
			<div class="departamento-edit-page-header-spacer" aria-hidden="true"></div>

		</header>

		{{ form_start(form, {
            action: path('app_departamento_edit', { id: departamento.id }),
            attr: {
                'data-turbo-frame': 'content',
                'class': 'departamento-edit-page-form'
            }
        }) }}

			<div class="departamento-edit-page-group">
				{{ form_label(form.nombre, null, {'label_attr': {'class': 'departamento-edit-page-label'}}) }}
				{{ form_widget(form.nombre, {'attr': {'class': 'departamento-edit-page-input'}}) }}
				{{ form_errors(form.nombre) }}
			</div>

			<div class="departamento-edit-page-group">
				{{ form_label(form.activo, null, {'label_attr': {'class': 'departamento-edit-page-label'}}) }}
				{{ form_widget(form.activo, {'attr': {'class': 'departamento-edit-page-select'}}) }}
				{{ form_errors(form.activo) }}
			</div>

			<div class="departamento-edit-page-actions">
				<button type="submit" class="departamento-edit-page-save-btn" title="Guardar cambios">
					<i class="bi bi-check-lg"></i>
				</button>
			</div>

		{{ form_rest(form) }}
		{{ form_end(form) }}

	</section>
</turbo-frame>