<form
    method="post"
    action="{{ path('app_departamento_delete', {'id': departamento.id}) }}"
    onsubmit="return confirm('¿Seguro que deseas eliminar este registro?');"
>
    <input
        type="hidden"
        name="_token"
        value="{{ csrf_token('delete' ~ departamento.id) }}"
    >

    <button
        type="submit"
        class="departamento-edit-module__delete-button"
        title="Eliminar"
        aria-label="Eliminar"
    >
        
    </button>
</form>
