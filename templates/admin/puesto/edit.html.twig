<turbo-frame id="content">
	<section class="puesto-edit-page-card">

		<header class="puesto-edit-page-header">

			<!-- VOLVER -->
			<a href="{{ path('app_puesto_index') }}"
			   data-turbo-frame="content"
			   data-turbo-action="replace"
			   class="puesto-edit-page-back-btn"
			   title="Volver">
				<i class="bi bi-arrow-left"></i>
			</a>

			<!-- TÍTULO + SUBTÍTULO -->
			<div class="puesto-edit-page-header-center">
				<h1 class="puesto-edit-page-title">
					Editar puesto
				</h1>

				<p class="puesto-edit-page-subtitle">
					Edita solo los campos necesarios
				</p>
			</div>

			<!-- ESPACIADOR -->
			<div class="puesto-edit-page-header-spacer" aria-hidden="true"></div>

		</header>

		{{ form_start(form, {
            action: path('app_puesto_edit', { id: puesto.id }),
            attr: {
                'data-turbo-frame': 'content',
                'class': 'puesto-edit-page-form'
            }
        }) }}

			<div class="puesto-edit-page-group">
				{{ form_label(form.nombre, null, {'label_attr': {'class': 'puesto-edit-page-label'}}) }}
				{{ form_widget(form.nombre, {'attr': {'class': 'puesto-edit-page-input'}}) }}
				{{ form_errors(form.nombre) }}
			</div>

			<div class="puesto-edit-page-group">
				{{ form_label(form.supervisorDirecto, null, {'label_attr': {'class': 'puesto-edit-page-label'}}) }}
				{{ form_widget(form.supervisorDirecto, {'attr': {'class': 'puesto-edit-page-select'}}) }}
				{{ form_errors(form.supervisorDirecto) }}
			</div>

			<div class="puesto-edit-page-group">
				{{ form_label(form.activo, null, {'label_attr': {'class': 'puesto-edit-page-label'}}) }}
				{{ form_widget(form.activo, {'attr': {'class': 'puesto-edit-page-select'}}) }}
				{{ form_errors(form.activo) }}
			</div>

			<div class="puesto-edit-page-actions">
				<button type="submit" class="puesto-edit-page-save-btn" title="Guardar cambios">
					<i class="bi bi-check-lg"></i>
				</button>
			</div>

		{{ form_rest(form) }}
		{{ form_end(form) }}

	</section>
</turbo-frame>